# Chatbot Web UI

React-based frontend for the Chatbot server.

## Setup

```bash
cd web
pnpm install
```

## Development

```bash
pnpm dev
```

## Build

```bash
pnpm build
```

The built files are copied to `internal/ui/dist/` by the Makefile in the parent directory.
